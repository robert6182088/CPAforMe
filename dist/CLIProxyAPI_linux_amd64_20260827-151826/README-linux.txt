# CLIProxyAPI Linux amd64 package

Run from this directory:

    chmod +x ./cli-proxy-api
    ./cli-proxy-api

This package includes config.yaml and static/management.html. The packaged config disables management panel auto-update, so the server uses the bundled management page directly.
